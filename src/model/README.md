# Model Code
Add your AI model or ML training code here.


waste_model.pth: trained model
train_model.py: train the model 
prepare_data.py: classify and prepare the data 
grade_images.py: test the model against images 


Requirements:
pip3 install torchvision opencv-python
Python 3.12.6

pip install numpy pandas pillow
pip install opencv-python